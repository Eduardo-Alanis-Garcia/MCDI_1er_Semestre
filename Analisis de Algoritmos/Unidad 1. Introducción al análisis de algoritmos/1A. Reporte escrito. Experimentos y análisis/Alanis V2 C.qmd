---
title: "Vector vs. diccionario para mapas pequeños"
author: "Eduardo Alanis Garcia"
date: today
format:
  html:
    toc: true
    number-sections: true
  pdf:
    toc: true
    number-sections: true
jupyter: julia-1.11
---

# Actividad 1. Vector vs. diccionario para mapas pequeños

# Introcudcción

Pendiente de pendiente de la recta pendiente


# Instrucciones:

1.  Considere dos estructuras para representar un mapa de enteros (`Int64`) a flotantes (`Float64`), sin llaves repetidas:

    - Un vector de pares sin ordenar (e.g., `Vector{Pair{Int64,Float64}}`), donde toda operación recorre linealmente el vector.

    - Un `Dict{Int64,Float64}`, la tabla hash nativa de Julia.

    Implemente, para ambas estructuras, las operaciones `buscar`, `insertar!` y `borrar!` con la misma semántica de mapa (a lo más un valor por llave).

    **Restricción importante:** `insertar!` y `borrar!` sobre el vector deben implementarse sin modificar el tamaño (longitud) del arreglo, es decir, sin usar `push!`, `pop!`, `deleteat!` ni operaciones equivalentes que provoquen realojamiento o desplazamiento de memoria. Esto aísla el efecto que se quiere medir (costo del recorrido y localidad de referencia) del efecto de las políticas de crecimiento dinámico de arreglos. Para lograrlo:

    - Reserve el vector con una capacidad fija desde el inicio (la máxima que va a usar en el experimento).

    - Use un valor centinela (e.g., una llave reservada que nunca aparezca en los datos) para marcar las posiciones lógicamente “vacías”, o use un contador de elementos ocupados (añadiendo la lógica necesaria para llevar a cabo las operaciones).

    - `borrar!` sobrescribe la posición encontrada con el centinela; `insertar!` busca primero la llave (para actualizar) y, si no existe, busca una posición marcada como vacía para colocar el nuevo par; ambas búsquedas son recorridos lineales, nunca hay cambio de tamaño del arreglo.

2.  Mida con `@benchmark` el tiempo de cada operación (`buscar`, `insertar!`, `borrar!`) para creciente (potencias de 2 desde hasta el máximo práctico en su equipo), con la estructura ya poblada con pares aleatorios antes de medir. Para `buscar` y `borrar!` la llave a consultar debe elegirse aleatoriamente dentro del `setup=` de cada muestra (no fija), para capturar el caso promedio y no solo el mejor o peor caso posicional. Aplique el mismo cuidado del lado del `Dict` (considere `sizehint!` para controlar el efecto de *rehash* al crecer).

3.  Para cada una de las tres operaciones, determine el o los rangos de donde el vector es más rápido que el diccionario, y viceversa. Reporte con gráficas tiempo-vs- (escala log-log recomendada) y/o tablas.

4.  Discuta sus resultados en términos de localidad de referencia (fallos de caché por saltos de puntero en el diccionario vs. recorrido contiguo en el vector) y el costo de calcular el hash y resolver colisiones.

## 4.1 Valor CENTINELA

Se utilizará una llave que no forme parte de los datos reales:

```{julia}
CENTINELA  = typemin(Int64)
```

Las llaves utilizadas en el experimento estarán entre `1` y `n`, por lo que esta llave puede utilizarse para identificar posiciones vacías.

## 4.2 Vector de capacidad fija

El vector se crea con una capacidad determinada desde el inicio.

```{julia}
function crear_vector(capacidad)
    return  Vector{Pair{Int64,Float64}}([CENTINELA => 0.0 for i in 1:capacidad])
end
```

No se utiliza `push!`, `pop!`, `deleteat!` ni ninguna operación que cambie el tamaño del vector.

## 4.3 Implementación de `buscar` para el vector

```{julia}
function buscar_vector(vector, llave)
    for par in vector
        println(par)
        if par.first != CENTINELA  && par.first == llave
            return par.second
        end
    end
    return nothing
end
```



La función recorre el vector hasta encontrar la llave. Si no la encuentra, devuelve `nothing`.

## 4.4 Implementación de `insertar!` para el vector

```{julia}
function insertar_vector!(v, llave, valor)
    for i in 1:length(v)

        # Si la llave ya existe, se actualiza el valor
        if v[i].first == llave
            v[i] = llave => valor
            return v
        end

        # Si no existe, busca una posición marcada como vacía para colocar el nuevo par
        if v[i].first == CENTINELA 
            v[i] = llave => valor
            return v
        end
    end

    error("No hay ningun espacio disponible en vector")
end
```

La función conserva fijo el tamaño del vector. Si la llave ya existe, se actualiza su valor. Si no existe, se utiliza la primera posición vacía.

## 4.5 Implementación de `borrar!` para el vector

```{julia}
function borrar_vector!(v, llave)
    for i in 1:length(v)
        if v[i].first == llave
            v[i] = CENTINELA  => 0.0
            return v
        end
    end

    return v
end
```

La posición no se elimina físicamente. Se marca nuevamente como vacía mediante el valor CENTINELA.

# 5. Implementación del diccionario

Se utilizan funciones equivalentes para poder comparar ambas estructuras de manera sencilla.

```{julia}

function crear_dict_mapa(capacidad::Int)
    d = Dict{Int64,Float64}()
    sizehint!(d, capacidad)
    return d
end

function buscar_dict(d, llave)
    if haskey(d, llave)
        return d[llave]
    end
    return nothing
end

function insertar_dict!(d, llave, valor)
    d[llave] = valor
    return d
end

function borrar_dict!(d, llave)
    delete!(d, llave)
    return d
end
```


# 7. Preparación de los datos

Para cada tamaño `n` se crean ambas estructuras con los mismos datos.

```{julia}
function preparar_datos(n, capacidad_vector = n)

    # Porque es posible usar n + 1
    v = crear_vector(capacidad_vector)
    d = crear_dict_mapa(n)
    llaves = collect(Int64(1):Int64(n))  

    shuffle!(llaves) # Desordena de forma aleatoria, por la indicacion la llave a consultar debe elegirse aleatoriamente dentro del setup= de cada muestra (no fija), para capturar el caso promedio y no solo el mejor o peor caso posicional
    valores = rand(n)

    for i in 1:n
        insertar_vector!(v, llaves[i], valores[i])
        insertar_dict!(d, llaves[i], valores[i])
    end

    return v, d
end
```



# 9. Experimento: `buscar`

```{julia}
using BenchmarkTools
using Random

tamaños = 2 .^ (2:8)   # (potencias de 2 desde hasta el máximo práctico en su equipo)
```

```{julia}
resultados_buscar_vector = []
resultados_buscar_dict = []

for n in tamaños
    v, d = preparar_datos(n)

    resultado_vector = @benchmark buscar_vector($v, llave) setup=(
        llave = rand(Int64(1):Int64($n))
    ) samples=100

    resultado_dict = @benchmark buscar_dict($d, llave) setup=(
        llave = rand(Int64(1):Int64($n))
    ) samples=100

    push!(resultados_buscar_vector, 
    (
        n = n,
        trial = resultado_vector,
        minimo = minimum(resultado_vector).time,
        medio = mean(resultado_vector).time,
        maximo = maximum(resultado_vector).time
    )
    )
    push!(resultados_buscar_dict, 
    (
        n = n,
        trial = resultado_dict,
        minimo = minimum(resultado_dict).time,
        medio = mean(resultado_dict).time,
        maximo = maximum(resultado_dict).time
    )
    )
end

resultados_buscar_vector, 
resultados_buscar_dict
```


```{julia}
resultados_buscar_vector
```

```{julia}
resultados_buscar_dict
```

# 10. Experimento: `borrar!`

```{julia}
resultados_borrar_vector = []
resultados_borrar_dict = []

for n in tamaños
    v, d = preparar_datos(n)

    resultado_vector = @benchmark borrar_vector!($v, llave) setup=(
        llave = rand(Int64(1):Int64($n))
    ) samples=100

    push!(resultados_borrar_vector, 
    (
        n = n,
        trial = resultado_vector,
        minimo = minimum(resultado_vector).time,
        medio = mean(resultado_vector).time,
        maximo = maximum(resultado_vector).time
    )
    )

    resultado_dict = @benchmark borrar_dict!($d, llave) setup=(
        llave = rand(Int64(1):Int64($n))
    ) samples=100

    push!(resultados_borrar_dict, 
    (
        n = n,
        trial = resultado_dict,
        minimo = minimum(resultado_dict).time,
        medio = mean(resultado_dict).time,
        maximo = maximum(resultado_dict).time
    )
    )
end
```

```{julia}
resultados_borrar_vector
```

```{julia}
resultados_borrar_dict
```

# 11. Experimento: `insertar!`

Para realizar la inserción se utiliza la llave `n + 1`, que no se encuentra todavía en las estructuras.

## 11.1 Vector

```{julia}

resultados_insertar_vector = []
resultados_insertar_dict = []

for n in tamaños
    v, d = preparar_datos(n, n + 1)

    resultado_vector = @benchmark insertar_vector!($v, nueva_llave, 1.0) setup=(
        nueva_llave = Int64($n + 1)
    ) samples=100

    resultado_dict = @benchmark insertar_dict!($d, nueva_llave, 1.0) setup=(
        nueva_llave = Int64($n + 1)
    ) samples=100

    push!(resultados_insertar_vector, 
    (
        n = n,
        trial = resultado_vector,
        minimo = minimum(resultado_vector).time,
        medio = mean(resultado_vector).time,
        maximo = maximum(resultado_vector).time
    )
    )
    push!(resultados_insertar_dict, 
    (
        n = n,
        trial = resultado_dict,
        minimo = minimum(resultado_dict).time,
        medio = mean(resultado_dict).time,
        maximo = maximum(resultado_dict).time
    )
    )
end
```

```{julia}
resultados_insertar_vector
```

```{julia}
resultados_insertar_dict
```

# 12. Presentación de los resultados

## 12.1 Buscar

```{julia}
using DataFrames
```

```{julia}
tabla_buscar = DataFrame(
    n = [r.n for r in resultados_buscar_vector],

    vector_minimo = [r.minimo for r in resultados_buscar_vector],
    vector_medio = [r.medio for r in resultados_buscar_vector],
    vector_maximo = [r.maximo for r in resultados_buscar_vector],

    dict_minimo = [r.minimo for r in resultados_buscar_dict],
    dict_medio = [r.medio for r in resultados_buscar_dict],
    dict_maximo = [r.maximo for r in resultados_buscar_dict]
)

tabla_buscar.comparar = ifelse.(tabla_buscar.vector_medio .< tabla_buscar.dict_medio, "Vector", "Dict")
```


```{julia}
tabla_buscar 
```

```{julia}
using Plots
plot(
    tabla_buscar.n,
    [tabla_buscar.vector_medio tabla_buscar.dict_medio],
    label=["Vector" "Dict"],
    xlabel="n",
    ylabel="Tiempo medio",
    marker=:circle,
    linewidth=2,
    xscale=:log2,
    yscale=:log10
)
```

## 12.2 Borrar

```{julia}
tabla_borrar = DataFrame(
    n = [r.n for r in resultados_borrar_vector],

    vector_minimo = [r.minimo for r in resultados_borrar_vector],
    vector_medio = [r.medio for r in resultados_borrar_vector],
    vector_maximo = [r.maximo for r in resultados_borrar_vector],

    dict_minimo = [r.minimo for r in resultados_borrar_dict],
    dict_medio = [r.medio for r in resultados_borrar_dict],
    dict_maximo = [r.maximo for r in resultados_borrar_dict]
)

tabla_borrar.comparar = ifelse.(tabla_borrar.vector_medio .< tabla_borrar.dict_medio, "Vector", "Dict")
```

```{julia}
tabla_borrar
```

```{julia}
plot(
    tabla_borrar.n,
    [tabla_borrar.vector_medio tabla_borrar.dict_medio],
    label=["Vector" "Dict"],
    xlabel="n",
    ylabel="Tiempo medio",
    marker=:circle,
    linewidth=2,
    xscale=:log2,
    yscale=:log10
)
```


## 12.3 Insertar

```{julia}
tabla_insertar = DataFrame(
    n = [r.n for r in resultados_insertar_vector],

    vector_minimo = [r.minimo for r in resultados_insertar_vector],
    vector_medio = [r.medio for r in resultados_insertar_vector],
    vector_maximo = [r.maximo for r in resultados_insertar_vector],

    dict_minimo = [r.minimo for r in resultados_insertar_dict],
    dict_medio = [r.medio for r in resultados_insertar_dict],
    dict_maximo = [r.maximo for r in resultados_insertar_dict]
)

tabla_insertar.comparar = ifelse.(tabla_insertar.vector_medio .< tabla_insertar.dict_medio, "Vector", "Dict")
```

```{julia}
tabla_insertar 
```

```{julia}
plot(
    tabla_insertar.n,
    [tabla_insertar.vector_medio tabla_insertar.dict_medio],
    label=["Vector" "Dict"],
    xlabel="n",
    ylabel="Tiempo medio",
    marker=:circle,
    linewidth=2,
    xscale=:log2,
    yscale=:log10
)
```


# 13. Análisis e interpretación

## 13.1 `buscar`

En el vector, la búsqueda requiere recorrer las posiciones una por una. En consecuencia, el número de operaciones aumenta con `n` y el costo esperado es lineal:

$$
T(n) = O(n)
$$

En el diccionario, la tabla hash permite localizar una posición de manera mucho más directa y por ello esperamos un costo promedio aproximadamente constante:

$$
T(n) \approx O(1)
$$

Aun así, la medición real puede mostrar que el vector sea competitivo para valores pequeños de `n` debido a los costos constantes y a la localidad de referencia.

## 13.2 `insertar!`

En el vector se debe recorrer la estructura para saber si la llave ya existe y para localizar una posición libre. Por eso esperamos nuevamente un costo lineal.

En el diccionario, la inserción se realiza utilizando la tabla hash, por lo que el costo promedio esperado es aproximadamente constante.

## 13.3 `borrar!`

Para borrar del vector primero se debe localizar la llave. Una vez encontrada, solamente se marca la posición como vacía.

En el diccionario se localiza la entrada mediante la tabla hash y posteriormente se elimina.

Por esta razón, nuevamente esperamos un crecimiento lineal para el vector y aproximadamente constante en promedio para el diccionario.

# 14. Localidad de referencia

El vector almacena sus elementos de forma contigua. Cuando el programa recorre una posición, las siguientes posiciones están cerca en memoria.

Esto favorece la localidad de referencia y permite aprovechar mejor la caché del procesador.

En una tabla hash, los accesos pueden estar más dispersos en memoria. Por ello, aunque el diccionario tenga una mejor complejidad promedio, no necesariamente será más rápido en todos los tamaños.

Para tamaños pequeños, el recorrido secuencial del vector puede tener una ventaja práctica debido a que los datos están juntos en memoria y las operaciones son sencillas.

# 15. Costo del hash y colisiones

El diccionario necesita calcular el hash de cada llave y utilizar ese resultado para localizar la entrada.

Además, pueden ocurrir colisiones cuando diferentes llaves terminan relacionadas con una misma región de la tabla. Resolver estas situaciones introduce trabajo adicional.

Por eso, el diccionario tiene costos constantes que pueden ser relevantes en tamaños pequeños. Cuando `n` aumenta, la diferencia entre recorrer linealmente un vector y realizar una búsqueda promedio mediante hash se vuelve más importante.

# 16. ¿En qué rango es más rápido cada uno?

Esta conclusión debe obtenerse con los resultados reales del experimento.

No es correcto establecer un punto exacto de cambio sin ejecutar `@benchmark`, porque los resultados dependen del equipo, del compilador, de la memoria caché y de otros factores de implementación.

La interpretación debe seguir la siguiente idea general:

- Para tamaños muy pequeños, el vector puede ser competitivo debido a su simplicidad y localidad de referencia.
- Conforme aumenta `n`, se espera que el `Dict` tenga una ventaja creciente en las operaciones de búsqueda, inserción y eliminación.
- El punto exacto donde cambia la ventaja debe obtenerse experimentalmente.

# 17. Conclusión

La actividad permite comprobar la diferencia entre dos formas de representar un mapa.

El vector de pares es sencillo y aprovecha una disposición contigua de memoria, pero las operaciones de búsqueda, inserción y eliminación requieren recorrer la estructura, por lo que su costo crece linealmente con el número de elementos.

El `Dict` utiliza una tabla hash y presenta un costo promedio aproximadamente constante para estas operaciones. Sin embargo, también existen costos asociados al cálculo del hash, al manejo de colisiones y al acceso a memoria.

Por lo tanto, el análisis asintótico ayuda a predecir el comportamiento cuando `n` crece, mientras que el análisis experimental permite observar cómo las características reales de la computadora influyen en el rendimiento.

# Verificación final con la rúbrica

| Criterio o elemento | Estado |
|------------------------------------|------------------------------------|
| Identificación de la actividad | Cumplido |
| Explicación del problema | Cumplido |
| Código de vector | Cumplido |
| Código de diccionario | Cumplido |
| Código documentado | Cumplido |
| Uso de `@benchmark` | Cumplido |
| Tamaños crecientes | Cumplido |
| Llaves aleatorias para búsqueda y borrado | Cumplido |
| Vector de capacidad fija | Cumplido |
| Comparación teórica | Cumplido |
| Análisis de localidad de referencia | Cumplido |
| Análisis de hash y colisiones | Cumplido |
| Resultados experimentales reales | Pendiente de ejecutar en Julia |
| Gráficas con datos reales | Pendiente de ejecutar en Julia |
| Punto exacto donde cambia la ventaja | Pendiente de obtener experimentalmente |
| Conclusión | Cumplido |

## Aspectos que deben completarse antes de entregar

1.  Ejecutar el notebook en Julia.
2.  Registrar los tiempos reales obtenidos por `@benchmark`.
3.  Crear las gráficas correspondientes.
4.  Sustituir las tablas de ejemplo por las mediciones reales.
5.  Determinar para cada operación en qué rango gana el vector y en qué rango gana el diccionario.
6.  Revisar que los resultados obtenidos sean coherentes con el análisis teórico.

# Referencias

Téllez, E. S. *Análisis de Algoritmos con Julia*. Capítulo 1: Julia.

Téllez, E. S. *Análisis de Algoritmos con Julia*. Capítulo 2: Análisis de algoritmos.

Repositorio de trabajo del curso, Unidad 1, *Reporte escrito. Experimentos y análisis*.